FROM python:3.13.1-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
# Add your LangChain app code later
# EXPOSE <port> (if needed)
# CMD ["python", "your_app.py"]
